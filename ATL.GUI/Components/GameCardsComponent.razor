@inherits ComponentBase

<div style="width: 100%; overflow-x: scroll" class="pa-5">
  <MudStack Row="true" Spacing="5" Style="width: fit-content">
    @foreach(var gameId in TestGameIds)
    {
      <GameCard GameID="@gameId" OnClick="OnGameCardClicked" Selected="@(gameId == SelectedGameId)"/>
    }
  </MudStack>
</div>
